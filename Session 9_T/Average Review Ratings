WITH CTE AS(
  SELECT EXTRACT(MONTH FROM SUBMIT_DATE) AS MTH, PRODUCT_ID AS PRODUCT,
  ROUND(AVG(stars) OVER(PARTITION BY EXTRACT(MONTH FROM SUBMIT_DATE), product_id),2)
  AS AVG_STARS
  FROM 
  REVIEWS
)
SELECT * FROM CTE GROUP BY MTH,PRODUCT , AVG_STARS ORDER BY MTH,PRODUCT
